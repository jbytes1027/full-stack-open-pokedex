name: hello world

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: say hi
        run: "echo 'Hi'"
      - name: record date
        run: 'date'
      - name: print dir
        run: 'ls -l'